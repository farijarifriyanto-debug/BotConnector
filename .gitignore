node_modules/
dist/
dist-sea/
*.log
.DS_Store

# Native Agent acceptance suite's isolated scratch userData (regenerated
# fresh on every run — never committed, unlike the seeded fixtures next to it)
tests/fixtures/.native-agent-userdata/

# Downloaded models (multi-GB, user data — never commit)
*.gguf
*.gguf.part
*.part
/models/
models/

# Managed runtime binaries/archives (reproducible via installer — never commit)
runtimes/
*.zip
*.tar.gz
llama-server.exe
llama-*.exe

# Secrets / user data (never commit)
.env
.env.*
*secret*
*token*
settings.json
cli-server.json
userData/

# Logs, caches, temp
logs/
.cache/
tmp/
temp/
*.bak-*
opencode.json
