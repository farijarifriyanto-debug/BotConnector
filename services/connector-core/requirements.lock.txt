# Reconstructed from proven live runtime
# Python: 3.12.3
# Purpose: reproducible production baseline
# Source provenance: see docs/provenance/SOURCE-MAP.md
#
# Canonical exact-pin lock for the ONE shared venv used by connector-core
# AND all 6 shipping services (confirmed via each systemd unit's
# ExecStart, which all point at this same venv's uvicorn binary):
#   services/connector-core (this directory)
#   services/shipping/integration
#   services/shipping/location-resolver
#   services/shipping/location-public-gateway
#   services/shipping/public-gateway
#   services/shipping/router
#   services/shipping/rajaongkir-cost
# This file complements, not replaces, the existing requirements.txt in
# this directory (loose/direct ranges — the declared intent). This one is
# the exact snapshot of what's actually installed and running.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
cryptography==49.0.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
starlette==1.6.0
typing_extensions==4.16.0
typing-inspection==0.4.4
uvicorn==0.52.1
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.0.1
