{
  "name": "botconnector-canary",
  "version": "1.0.0",
  "description": "Canary test suite for BotConnector VPS AI infrastructure",
  "main": "dist/src/canary/routing-test.js",
  "workspaces": [
    "packages/contracts"
  ],
  "scripts": {
    "build": "tsc",
    "build:contracts": "npm run build --workspace @botconnector/contracts",
    "typecheck:contracts": "npm run typecheck --workspace @botconnector/contracts",
    "test:contracts": "npm run test --workspace @botconnector/contracts",
    "schema:contracts": "npm run schema --workspace @botconnector/contracts",
    "test": "vitest run",
    "test:watch": "vitest",
    "canary": "./scripts/canary-build.sh"
  },
  "keywords": [
    "canary",
    "testing",
    "ai",
    "botconnector"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vitest": "^1.1.0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
