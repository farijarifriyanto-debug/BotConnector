[build-system]
requires = []
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "botconnector-local-source-sdk"
version = "0.4.0"
description = "Unpublished local-source Python boundary for BotConnector Core"
requires-python = ">=3.9"
dependencies = []
